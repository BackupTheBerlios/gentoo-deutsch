<?xml version='1.0' encoding="UTF-8"?>

<guide link="/doc/de/gentoo-ppc-install.xml">
<title>Gentoo Linux/PPC 1.2 Installationsanleitung</title>
<author title="Gentoo PPC Developer"><mail link="gerk@gentoo.org">Mark Guertin</mail></author>
<author title="Gentoo PPC Developer"><mail link="daybird@gentoo.org">David Chamberlain</mail></author>
<author title="Chief Architect"><mail link="drobbins@gentoo.org">Daniel Robbins</mail></author>                              
<author title="Author">Chris Houser</author>                                                                                 
<author title="Author"><mail link="jerry@gentoo.org">Jerry Alexandratos</mail></author>                                      
<author title="Ghost"><mail link="g2boojum@gentoo.org">Grant Goodyear</mail></author> 
<author title="Contributor, Maintainer"><mail link="rajiv@gentoo.org">Rajiv Manglani</mail></author>
<author title="Contributor"><mail link="zhen@gentoo.org">John P. Davis</mail></author>
<author title="Übersetzer"><mail link="pylon@chaosdorf.de">Lars Weiler</mail></author>

<abstract>Diese Anleitung führt schrittweise durch die Gentoo Linux/PPC 1.2
Installation für PowerPC-Prozessoren.  Die Gentoo Linux Installation bietet
verschiedene Installationsarten, ganz davon abhängig, inwieweit das System
von Grund auf gebaut werden soll.</abstract>

<version>1.0</version>
<date>17 February 2003</date>

<chapter>
<title>Über die Installation</title>
<section>
<body>

<p>Die neue Boot-CD lässt sich auf jedem NewWorld Mac booten.  OldWorld
Macs benötigen eine funktionierende MacOS-Partition und den <c>Gentoo
Installer.sit</c>, der sich im ISO befindet.  Zusätzlich bieten wir
Kernel-Treiber für einen großen Teil an Apple-Hardware und Programme
zum Einrichten des Netzwerks, Aufbauen einer <c>ssh</c>-Verbindung oder zum
Downloaden von Dateien.</p>

<p>Um von der "Build-CD" zu installieren wird mindestens ein PowerPC 603
Prozessor auf einem PowerPC PCI basiertem System mit 128 MB RAM benötigt
(weniger ist möglich, aber die Installation kann schrecklich langsam werden
oder sogar komplett versagen).  Nubus-Maschinen werden noch nicht
unterstützt.  Um mit der Installation zu beginnen, ist eines unserer CD ISO
Images von <uri>http://www.ibiblio.org/gentoo/releases/build/</uri>
erforderlich.  Für die Installation von Gentoo Linux/PPC 1.4 gibt es nicht
nur gewöhnliche PPC-Images, sondern auch für G4-Prozessoren
optimierte.</p>

<p>Nun lassen Sie uns einen kurzen Überblick des Installationsprozesses
skizzieren: Sie erstellen Partitionen, dann die Dateisysteme und entpacken darin
entweder ein stage1, stage2 oder stage3 tar-Archiv.  Falls Sie ein stage1 oder
stage2 tar-Archiv verwenden, sind entsprechende Schritte um zu stage3 zu
gelangen erforderlich.  Sobald das System in einem stage3-Status ist,
können Sie es konfigurieren (die Konfigurationsdateien anpassen, den
Bootloader installieren, usw.) und ein voll funktionsfähiges Gentoo Linux
System booten.  Abhängig von Ihrer gewählten Installationsstufe werden
die folgende Schritte benötigt:</p>

<p>
<table>
<tr><th>stage tar-Archiv</th><th>Installationsanforderungen</th></tr>
<tr><ti>1</ti><ti>Partitionen und Dateisystem erstellen, emerge rsync, bootstrap, emerge system, emerge ppc-sources, abschließende Konfiguration</ti></tr>
<tr><ti>2</ti><ti>Partitionen und Dateisystem erstellen, emerge rsync, emerge system, emerge ppc-sources, abschließende Konfiguration</ti></tr>
<tr><ti>3</ti><ti>Partitionen und Dateisystem erstellen, emerge rsync (optional), abschließende Konfiguration</ti></tr>
</table>
</p>

</body>
</section>
</chapter>

<chapter>
<title>Booten</title>
<section>
<body>

<p>Haben Sie sich für eine CD entschieden, booten Sie diese indem Sie
entweder die 'C'-Taste während des Startens gedrückt halten oder bei
OldWorld-Maschinen das Installationsprogramm aufrufen und dann BootX starten.
Sie werden mit jeder Menge Text und einem <c>boot:</c>-Prompt am Ende des
Bildschirms begrüßt.  Drücken Sie dort Enter und Linux wird von
der CD starten.</p>

<p>Darauf wird Ihnen eine kleine Liste der verfügbaren Befehle auf der
Boot-CD, inklusive <c>nano</c> (einem Klon des Editors <c>pico</c>) und
Anweisungen zur Netzwerkkonfiguration, angezeigt.  Dann werden Sie zur Wahl
Ihrer Tastaturbelegung aufgefordert, gefolgt von der automatischen Erkennung der
PCI-Geräte.  Die Geräteerkennung wird automatisch die passenden
Kernelmodule für die gängigen PCI SCSI-Controler und Netzwerkkarten
laden.  Ist dieses erfolgt, können Sie Enter drücken und gelangen in
eine minimale Boot-CD Linux-Umgebung.  Sie sollten in der aktuellen Konsole ein
root-Prompt ("<c>#</c>") sehen.  Weitere Konsolen können mittels
ctrl+alt-F2, ctrl+alt-F3 (bei PowerBooks/iBooks möglicherweise
fn+ctrl+alt-F2, fn+ctrl+alt-F3) usw. gefolgt von einem Enter aktiviert
werden.</p>


</body>
</section>
</chapter>

<chapter>
<title>Laden von Kernel-Modulen</title>
<section>
<body>

<p>Nun können Sie nahezu mit der Installation von Gentoo Linux beginnen,
denn der Gentoo PPC ISO Kernel behinhaltet jegliche Treiber für die
meistens Standard-Macs.  Für einige zusätzliche Geräte haben wir
weitere Module bereitgestellt.  Sie können sich im Verzeichnis
<path>/lib/modules/&lt;kernel-version&gt;/kernel/drivers/</path> einen Überblick
über diese verschaffen.  Um ein spezielles Modul zu laden, geben Sie ein:</p>

<pre>
# <e>modprobe &lt;Modulname&gt;</e>
	<comment>(ersetzen Sie &lt;Modulname&gt; durch Ihr gewünschtes Modul)</comment>
</pre>

</body>
</section>
</chapter>

<chapter>
<title>Konfigurieren und Installieren des Netzwerks</title>
<section>
<body>

<p>Die neue Boot-CD erlaubt Ihnen das Netzwerk zu konfigurieren und Programme
wie <c>ssh</c>, <c>scp</c> oder <c>wget</c> selbst vor dem Installationsbeginn
zu benutzen.  Wenn Sie jetzt keine dieser Programme benötigen, können
Sie dennoch schon das Netzwerk konfigurieren.  Sobald es einmal eingestellt ist
kann Portage während des Installationsprozesses in der
<c>chroot</c>-Umgebung darauf zugreifen (wird für die Installation von
Gentoo Linux benötigt).</p>

</body>
</section>

<section>
<title>DHCP</title>
<body>

<p>Netzwerkkonfiguration mittels DHCP ist recht einfach.  Sollte Ihr
Internet-Anbieter kein DHCP bereitstellen, fahren Sie mit der <uri
link="#doc_chap4_sect3">statischen Netzwerkkonfiguration</uri> fort.</p>

<pre caption="Netzwerkkonfiguration mit DHCP">
# <e>dhcpcd eth0</e>
</pre>
<note>
Einige Provider erwarten von Ihnen einen Hostnamen.  Dafür hängen Sie
die Option <c>-h myhostname</c> an das oben stehende dhcpcd-Kommando.
</note>

<p>Falls Sie <e>dhcpConfig</e>-Warnungen erhalten, keine Panik -- die meisten
Meldungen sind harmlos.  Fahren Sie mit dem <uri link="#doc_chap4_sect4">Testen
der Netzwerkkonfiguration</uri> fort.</p>

</body>
</section>

<section>
<title>Statische Netzwerkkonfiguration</title>
<body>

<p>Sie müssen Ihre Netzwerkanbindung so weit konfigurieren, dass Sie die
benötigten Quellen für den Installationsprozess herunterladen
können.  Außerdem wird das lokale Interface (localhost) benötigt.
Bitte geben Sie dazu folgende Befehle in und ersetzen Sie <c>$IFACE</c> durch
Ihr Netzwerkinterface (üblicherweise <c>eth0</c>), <c>$IPNUM</c> durch Ihre
IP-Adresse, <c>$BCAST</c> durch Ihre Broadcast-Adresse und <c>$NMASK</c> durch
Ihre Netzwerkmaske.  Beim <c>route</c>-Befehl ersetzen Sie <c>$GTWAY</c> durch
Ihren Default-Gateway.</p>

<pre>
# <e>/sbin/ifconfig $IFACE $IPNUM broadcast $BCAST netmask $NMASK</e>
# <e>/sbin/route add -net default gw $GTWAY netmask 0.0.0.0 metric 1</e>
</pre>

<p>Nun sollten Sie die Datei <path>/etc/resolv.conf</path> erstellen, damit die
Namensauflösung (Web/FTP-Seiten mittels Namen anstatt IP-Adressen finden)
funktioniert.</p>

<note>Derzeit ist der einzige standardmäßig installierte Editor
<c>nano</c>, ein kleiner und leicht zu benutzender E</note>

<note>Im Moment ist nur <c>nano</c>, ein kleiner, leicht zu bedienender Editor installiert.
Verwenden Sie <c>nano</c> mit der Option <c>-w</c>, um den automatischen Zeilenumbruch
abzuschalten.</note>

<p>Hier ist ein Beispiel der Datei <path>/etc/resolv.conf</path>:</p>

<pre caption="/etc/resolv.conf Vorlage">
domain mydomain.com
nameserver 10.0.0.1
nameserver 10.0.0.2
</pre>

<p>Ersetzen Sie <c>10.0.0.1</c> und <c>10.0.0.2</c> durch die IP-Adressen Ihres
primären und sekundären Direct-Name-Servers (DNS).</p>

</body>
</section>

<section>
<title>Testen der Netzwerkkonfiguration</title>
<body>

<p>Da Ihre Netzwerkkonfiguration jetzt durchgeführt ist, sollte der Befehl
<c>/sbin/ficonfig -a</c> ausgeben, dass Ihre Netzwerkkarte (hoffentlich)
funktioniert (achten Sie auf <e>UP</e> und <e>RUNNING</e> in der Ausgabe!).</p>

<pre caption="/sbin/ifconfig bei einer funktionierenden Netzwerkkonfiguration">
eth0      Link encap:Ethernet  HWaddr 00:50:BA:8F:61:7A
          inet addr:192.168.0.2  Bcast:192.168.0.255  Mask:255.255.255.0
          inet6 addr: fe80::50:ba8f:617a/10 Scope:Link
          UP BROADCAST RUNNING MULTICAST  MTU:1500  Metric:1
          RX packets:1498792 errors:0 dropped:0 overruns:0 frame:0
          TX packets:1284980 errors:0 dropped:0 overruns:0 carrier:0
          collisions:1984 txqueuelen:100
          RX bytes:485691215 (463.1 Mb)  TX bytes:123951388 (118.2 Mb)
          Interrupt:11
</pre>
<!--
<note>
If you have a static ppp link, you can !!! do this...!!!

Siehe auch die Anmerkungen zu diesem Punkt in de/gentoo-x86-install.xml
Ich besitze kein DSL mit PPPoE und von daher keine Konfiguration beschreiben (LW)
</note>
-->
</body>
</section>

<section>
<title>Das Netzwerk ist jetzt online!</title>
<body>
<p>Der Netzwerkanschluss ist nun konfiguriert und nutzbar. Sie sind jetzt in der
Lage, sich via <c>ssh</c>, <c>scp</c> und <c>wget</c> mit anderen Rechnern in
Ihrem LAN oder im Internet zu verbinden.</p>
</body>
</section>
</chapter>

<chapter>
<title>Erstellen der Partitionen</title>
<section>
<body>

<p>Da nun das Netzwerk und der Festplatten-Controler angesprochen werden
können, ist es an der Zeit die Festplatte für Gentoo Linux zu
partitionieren.</p>

<p>Hier ist ein kurzer Überblick der Standard Gentoo Linux Partitionen:
Wir erzeugen mindestens drei Partitionen: eine Swap Partition (wird als
erweiterter Arbeitsspeicher benötigt), eine Root Partition (dort wird sich
der Großteil von Gentoo Linux befinden) und eine spezielle Bootstrap
Partition.  Die Bootstrap Partition wird die Informationen des YABOOT
Boot-Managers speichern.  Benutzer von OldWorld-Maschinen benötigen die
Bootstrap Partition nicht, da ihr System zuerst MacOS booten wird, um dann mit
BootX den Linux-Kernel und die Startskripte zu initiieren.</p>

<p>Nun zu den diversen Dateisystemen.  Momentan haben Sie vier Dateisysteme zur
Wahl: XFS, ext2, ext3 (Journaling) und ReiserFS.  ext2 ist ein erptrobtes und
das "wahre" Linux Dateisystem, jedoch unterstützt es kein Journaling.  ext3
ist die neue Version von ext2, sowohl mit Metadaten Journaling als auch
geordnete Schreibzugriffe und einem effektiven Daten-Journaling.  Eines der
beiden sollten Sie für Gentoo Linux/PPC verwenden.  ReiserFS und XFS sind
neue Dateisysteme mit guter Leistung, jedoch sind sie auf PPC-Systemen noch in
der Erprobungsphase und somit nicht empfohlen.</p>

<impo>Falls Sie bisher <e>nur</e> MacOS/MacOS X auf Ihrem System laufen haben,
müssen Sie Ihr komplettes System neu installieren.  Es gibt keine
Möglichkeit die MacOS-Partition(en) zu verkleinern.  Weitere
deutschsprachige Informationen zur Partitionierung gibt es z.B. unter
<uri>http://jules.killall5.de/writings/pismo/</uri>.</impo>

<warn>Machen Sie vor dem Partitionieren ein Backup Ihrer auf der Festplatte
gespeicherten Daten!  Durch das Partitionieren können diese unwiederruflich
gelöscht werden.</warn>

<note>Erstellen Sie nun Ihre Partitionen mit dem Programm mac-fdisk, indem Sie
<c>mac-fdisk /dev/hdx</c> eingeben (wobei x der Buchstabe Ihrer Festplatte ist,
also a, b, c usw., bzw. verwenden Sie /dev/sdx für SCSI-Festplatten).  Sie
benötigen mindestens eine Bootstrap Partition (<c>b</c> in mac-fdisk -- nur
für NewWorld-Benutzer), eine Swap Partition (in etwa der doppelten
Größe des RAM, jedoch sollte sie mindestens 512 MB betragen, falls Sie
größere Pakete auf Ihrem System kompilieren möchten) und eine
Root Partition. <c>?</c> am <c>mac-fdisk</c>-Prompt gibt Ihnen eine Liste der
verfügbaren Kommandos aus.</note>

<p>Da Sie nun ihre Partitionen mit <c>mac-fdisk</c> erstellt haben, ist es an
der Zeit die Dateisysteme für die Daten zu erzeugen.  Die Swap Partition
erstellen Sie mit</p>
<pre>
# <e>mkswap /dev/&lt;Swap Partition, z.B. hda3&gt;</e>
</pre>

<p>Sie können <c>mke2fs</c> zum Erzeugen von ext2-Dateisystemen verwenden:</p>
<pre>
# <e>mke2fs /dev/&lt;Root Partition, z.B. hda4&gt;</e>
</pre>

<p>Falls Sie ext3 verwenden möchten, verwenden Sie <c>mke2fs -j</c>:</p>
<pre>
# <e>mke2fs -j /dev/&lt;Root Partition, z.B. hda4&gt;</e>
</pre>

<warn>Noch einmal: Wir empfehlen <e>nicht</e> ReiserFS und XFS zu verwenden!
Obwohl ReiserFS und XFS auf vielen Systemen einwandfrei funktionieren, ist der
häufigste Grund von Datenverlust auf PPC-Systemen eine Kombination aus
ReiserFS oder XFS  mit einem 2.4er-Kernel.</warn>

<note>Mehr Informationen über ext3 unter Linux 2.4 finden Sie unter
<uri>http://www.zip.com.au/~akpm/linux/ext3/ext3-usage.html</uri>.</note>

</body>
</section>
</chapter>

<chapter>
<title>Partitionen mounten</title>
<section>
<body>

<p>Aktivieren Sie nun Ihre Swap Partition, denn es könnte sein, dass Sie den
virtuellen Speicher später noch benötigen:</p>
<pre>
# <e>swapon /dev/&lt;Swap Partition, z.B. hda3&gt;</e>
</pre>

<p>Als nächstes erstellen Sie das Verzeichnis <path>/mnt/gentoo</path> und
mounten Sie dorthin Ihre Root Partition:</p>
<pre>
# <e>mkdir /mnt/gentoo</e>
# <e>mount /dev/&lt;Root Partition, z.B. hda4&gt; /mnt/gentoo</e>
</pre>

<p>Falls Sie für Gentoo Linux weitere Partitionen wie <path>/usr</path>,
<path>/home</path> oder <path>/var</path> vorgesehen haben, mounten Sie diese
nach <path>/mnt/gentoo/usr</path>, <path>/mnt/gentoo/home</path> bzw. <path>/mnt/gentoo/var</path>.</p>

</body>
</section>
</chapter>

<chapter>
<title>Mounten des CD-ROM</title>
<section>
<body>

<p>Obwohl Sie von der CD-ROM gebootet haben, ist diese noch nicht in unserer
kleinen Linux-Umgebung gemountet.  Um auf das komprimierte Installations-Archiv
zuzugreifen (natürlich können Sie dieses auch aus dem <uri
link="http://distro.ibiblio.org/pub/Linux/distributions/gentoo/releases/build/">Internet
herunterladen</uri>, wenn Ihr <uri link="#doc_chap4">Netzwerk</uri>
funktioniert), müssen Sie die CD-ROM mounten.  Dazu tippen Sie folgendes
ein:</p>
<pre>
# <e>mount /dev/cdroms/cdrom0 /mnt/cdrom -o ro -t iso9660</e>
</pre>

<p>Auf der gemounteten CD sollten Sie die Datei <c>stage???.tbz2</c> mittels
<c>ls /mnt/cdrom/build</c> sehen.</p>

<note>Falls Sie Ihr CD-ROM nicht mounten können, laden Sie sich das
Installations-Archiv mit dem Programm <c>wget</c> von der ibiblio-Webseite
herunter, z.B. durch Eingabe von <c>wget
http://www.ibiblio.org/pub/Linux/distributions/gentoo/releases/build/1.2/stage1-ppc-1.2.tbz2</c>.</note>
</body>
</section>
</chapter>

<chapter>
    <title>Entpacken des gewünschten Installations-Archivs</title>
<section>
<body>

<p>Nun ist es an der Zeit das gewünschte, gepackte Installations-Archiv
nach <path>/mnt/gentoo</path> zu entpacken.  Dann wechseln Sie mit <c>chroot</c>
in die neue Gentoo Linux Installtion.</p>

<impo>Achten Sie darauf, dass Sie das Archiv mit der Option <c>p</c> des
Programms <c>tar</c> entpacken.  Andernfalls werden die Rechte einiger Dateien
nicht stimmen.</impo>

<impo>Wenn Sie die "bau alles von Grund auf neu" (stage1) Installation
gewählt haben, verwenden Sie das <path>stage1-ppc-1.2.tbz2</path>-Archiv.
Natürlich können Sie auch mit stage2 oder stage3 beginnen, um Zeit auf
Kosten der Flexibilität zu sparen (wir haben die Compiler Optimierung und
<c>USE</c>-Variablen für Sie ausgewählt).  Das stage3-Archiv
beinhaltet zusätzlich eine Kopie des "Portage tree", das ein späteres
<c>emerge rsync</c> überflüssig macht.</impo>

<pre>
# <e>cd /mnt/gentoo</e>
# <e>tar -xvjpf /mnt/cdrom/build/stage?-*.tbz2</e>
# <e>mount -o bind /proc /mnt/gentoo/proc</e>
# <e>cp /etc/resolv.conf /mnt/gentoo/etc/resolv.conf</e>
</pre>

<pre>
# <e>chroot /mnt/gentoo /bin/bash</e>
# <e>env-update</e>
&gt;&gt;&gt; Regenerating /etc/ld.so.cache...
# <e>source /etc/profile</e>
#
</pre>

<p>Nachdem Sie diese Kommandos ausgeführt haben, befinden Sie sich
"innerhalb" Ihrer neuen Gentoo Linux Umgebung.</p>

</body>
</section>
</chapter>

<chapter>
<title>Rsync</title>
<section>
<body>

<p>Als nächstes wird ein Durchlauf von <c>emerge rsync</c> benötigt.
Dabei wird der Server rsync.gentoo.org kontaktiert und die neueste Version des
Portage-Verzeichnisses heruntergeladen:</p> 

<pre>
# <e>emerge rsync</e>
</pre>

<p>Das ungefähr 10 MB große Portage-Verzeichnis wird nun
heruntergeladen.</p>

<impo>Die Adresse des rsync-Servers ist momentan
<path>rsync://rsync.gentoo.org/gentoo-portage</path>.  Falls Sie Probleme haben
auf Gentoo rsync zuzugreifen, überprüfen Sie, dass die SYNC Variable
in der Datei <path>/etc/make.conf</path> auf diese Adresse gesetzt ist.</impo>

<note>Sie können für den rsync-Befehl einen HTTP-Proxy einstellen,
falls Sie sich hinter einer Firewall befinden.  Dazu setzen Sie die Variable
<c>RSYNC_PROXY="hostname:port"</c> in der Datei <path>/etc/make.conf</path> oder
verwenden Sie diese als Umgebungsvariable.</note>

</body>
</section>
</chapter>

<chapter>
<title>Der Schritt von "stage1" zu "stage2"</title>
<section>
<body>

<impo>Falls Sie eine stage2- oder stage3-Archiv verwenden, dann haben wir den
bootstrap-Prozess bereits für Sie durchgeführt.  Es gibt keinen Grund,
den bootstrap-Prozess ein weiteres Mal durchzuführen, außer, Sie
entscheiden sich dafür nach dem <c>emerge rsync</c> die allerneuesten
Gentoo Linux Komponenten zu installieren.  Die meisten Benutzer, die stage2-
oder stage3-Archive verwenden, werden <e>nicht</e> noch einmal den
bootstrap-Prozess durchführen, da dieser selbst auf schnellen Rechnern
bis zu einer Stunde in Anspruch nehmen kann.  Dennoch empfehlen wir Ihnen die
Lektüre dieses Kapitels und die Einstellungen in der Datei
<path>/etc/make.conf</path> vorzunehmen.</impo>

<p>Da Sie nun eine funktionierende Kopie des Portage-Verzeichnisses haben,
müssen Sie nun den Gentoo Linux bootstrap-Prozess durchlaufen.  Dazu
editieren zuerst die Datei <path>/etc/make.conf</path>.  In dieser Datei sollten
Sie die <c>USE</c> Flags (mehr dazu in der <uri
link="/doc/de/use-howto.xml">Gentoo Anleitung zu den USE Flags</uri>) an Ihr
System und Ihre Wünsche anpassen.  Über die <c>USE</c> Flags
können Sie steuern, welche optionale Funktionalität Sie in die Pakete
eingebaut haben möchten.  Normalerweise ist die vorgegebene Einstellung,
<e>leere</e> oder ungesetzte <c>USE</c> Flags, ausreichend.  Wichtig sind die
für Ihr System passenden Einstellungen für <c>CHOST</c>, <c>CFLAGS</c>
und <c>CXXFLAGS</c>.  Auskommentierte Beispiele befinden sich in der Datei.
Falls Sie sich hinter einer Firewall befinden, sollten Sie einen Proxy
eintragen.</p>

<pre>
# <e>nano -w /etc/make.conf</e> <comment>(Passen Sie die Einträge an)</comment>
</pre>

<note>Anwender, die irh System wesentlich optimieren wollen oder müssen,
sollten einen Blick in die Date <path>/etc/make.globals</path> werfen.  Diese
Datei beinhaltet alle Gentoo Linux Standardeinstellungen und sollte niemals
verändert werden.  Falls die Standardeinstellungen nicht ausreichend sind,
können Sie neue Variablen in <path>/etc/make.conf</path> setzen, da
Einträge in der <path>make.conf</path> die vorgegebenen Einträge in
der <path>make.globals</path> <e>überschreiben</e>.  Falls Sie sich
für das Optimieren mittels der <c>USE</c> Flags interessieren, schauen Sie
in die Datei <path>/etc/make.profile/make.defaults</path>.  Wenn Sie vorhandene
<c>USE</c> Flags abschalten wollen, fügen Sie in der Datei
<path>/etc/make.conf</path> ein entsprechendes <c>USE="-foo"</c> ein (dieses
würde das Flag <c>foo</c> abschalten).</note>

<p>Jetzt können Sie den bootstrap-Prozess starten.  Dieser benötigt
etwa ein bis vier Stunden, ganz davon abhängig, wie fix Ihr Rechner ist.
Dabei wird das entpackte Installationsarchiv dazu vorbereitet, den Rest des
Systems zu kompiliren.  Die GNU Compiler Suite sowie die GNU C Bibliothek werden
gebaut.  Dieses sind zeitaufwändige Vorgänge, die den Hauptteil des
bootstrap-Prozesses ausmachen:</p>

<pre>
# <e>cd /usr/portage</e>
# <e>scripts/bootstrap.sh</e>
</pre>

<p>Der bootstrap-Prozess startet nun.</p>

<note>Portage verwendet normalerweise <path>/var/tmp</path> während des
Kompilieren der Pakete, das mehrere hundert MegaByte an temporärem Speicher
benötigt.  Wenn Sie das Verzeichnis, in das Portage diese temporären
Dateien ablegt, ändern möchten, können Sie dieses <e>vor</e> dem
Starten des bootstrap-Prozesses durch Setzen der Umgebungsvariable
<c>PORTAGE_TMPDIR</c> tun:</note>

<pre>
# <e>export PORTAGE_TMPDIR="/otherdir/tmp"</e>
</pre>

<p>Das Skript <c>bootstrap.sh</c> baut nun die Pakete <c>binutils</c>,
<c>gcc</c>, <c>gettext</c> und <c>glibc</c>.  Danach werden <c>binutils</c>,
<c>gcc</c> sowie <c>gettext</c> mit der neuen <c>glibc</c> neu gebaut.  Es muss
nicht erwähnt werden, dass dieser Prozess seine Zeit braucht...  Mache Sie
ein kleines Nickerchen und wenn der Prozess dann durchgelaufen ist, haben Sie
den Stand des stage2-Archivs erreicht.</p>

</body>
</section>
</chapter>

<chapter>
<title>Der Schritt von stage2 zu stage3</title>
<section>
<body>

<note>Falls Sie es noch nicht getan haben, editieren Sie die Datei
<path>/etc/make.conf</path> nach Ihren Vorlieben (siehe auch Kapitel <uri
link="#doc_chap10">Der Schritt von stage1 zu stage2</uri>).</note>

<p>Sobald der bootstrap-Prozess abgeschlossen ist und Sie sich in stage2
befinden (nochmals, wenn Sie ein stage3-Archiv verwenden, dann werden diese
Schritte nicht benötigt), kompilieren oder installieren Sie den Rest des
Basissystems wie folgt:</p>

<pre>
# <e>export CONFIG_PROTECT=""</e>
# <e>emerge --pretend system</e>
	<comment>[listet alle zu installierenden Pakete auf]</comment>
# <e>emerge system</e>
</pre>

<note>Die Umgebungsvariable <c>export CONFIG_PROTECT=""</c> stellt sicher, dass
alle neuen Dateien, die in das Verzeichnis <path>/etc</path> installiert werden,
die alten dort überschreiben (aus dem Paket
<path>sys-apps/baselayout</path>).  Dazu wird Portages neuer Mechanismus zum
Schutz von Konfigurationsdateien umgangen.  Genaueres erfahren Sie, wenn Sie
<c>emerge --help config</c> aufrufen.</note>

<p>Es dauert seine Zeit das ganze Basissystem zu installieren.  Ihr Lohn ist es,
dass es gänzlich auf Ihren Rechner optimiert ist.  Leider müssen Sie
einen Weg finden, sich bis zu diesem Zeitpunkt zu beschäftigen.  Der Autor
empfielt "Star Wars - Super Bombad Racing" für die PS2.  Wenn <c>emerge
system</c> abgeschlossen ist, besitzen Sie ein stage3 Gentoo Linux System.</p>

</body>
</section>
</chapter>

<chapter>
<title>Letzte Schritte: Zeitzone</title>
<section>
<body>

<note>Falls Sie es noch nicht getan haben, editieren Sie die Datei
<path>/etc/make.conf</path> nach Ihren Vorlieben (siehe auch Kapitel <uri
link="#doc_chap10">Der Schritt von stage1 zu stage2</uri>).</note>

<p>Sie sollten ein stage3 System haben, das zur abschließenden
Konfiguration bereit ist.  Sie beginnen mit dem Einstellen der Zeitzone.  Durch
setzen der Zeitzone bevor der Kernel gebaut wird, stellen Sie sicher, dass Sie
eine sinnvolle Ausgabe von <c>uname -a</c> erhalten.</p>

<p>Sie finden Ihre Zeitzonendatei in <path>/usr/share/zoneinfo/</path>.
Erstellen Sie nun einen symbolischen Link:</p>

<pre>
# <e>ln -sf /usr/share/zoneinfo/pfad/zur/zeitzonendatei /etc/localtime</e>
<comment>z.B. für Berlin:</comment>
# <e>ln -sf /usr/share/zoneinfo/Europe/Berlin /etc/localtime</e>
</pre>

</body>
</section>
</chapter>

<chapter>
<title>Letzte Schritte: Kernel, System Logger und Cron</title>
<section>
<body>

<p>Sie werden nun die ppc-Kernel-Quellen mit <c>emerge</c> installieren.  Dazu
geben Sie <c>emerge sys-kernel/ppc-sources</c> ein.</p>

<p>Sobald diese installiert sind, wird es an der Zeit, den eigenen Kernel zu
bauen:</p>

<pre>
# <e>cd /usr/src/linux</e>
# <e>make menuconfig</e>
# <e>make dep &amp;&amp; make clean vmlinux modules modules_install</e>
# <e>cp vmlinux System.map /boot</e>
</pre>

<note>Sie sollten <e>nur</e> die <c>sys-kernel/ppc-sources</c> Gentoo Kernel
für PowerPC-basierte Maschinen benutzen.</note>

<warn>Für einen fehlerfrei funktionierenden Kernel, stellen Sie sicher,
dass manche Optionen <e>nicht als Modul</e> kompiliert werden.  Dazu aktivieren
Sie <e>"Code maturity level options --> Prompt for development and/or incomplete
code/drivers"</e>, damit alle nötigen Optionen verfügbar sind.  In der
<e>"File systems"</e>-Sektion aktivieren Sie <e>"Device File System"</e>
(beachten Sie, dass Sie die <e>"/dev/pts file system support"</e> Option
<e>nicht</e> aktivieren müssen).  Außerdem müssen Sie das
<e>"Virtual Memory File system"</e> einschalten.  Dann noch unter <e>"Macintosh
Device Drivers"</e> den <e>"Support for ADB row keycodes"</e> abschalten.
Ebenso deaktivieren Sie die <e>"Enhanced RTC"</e> Option (in <e>"Character
devices"</e>), da es keine PPC-freundliche ATM ist.  Aber schalten Sie den
<e>"Support for /dev/rtc"</e> unter <e>"General setup"</e> ein.  Außerdem
ist es immer eine gute Idee, ext2 weiterhin aktiviert zu lassen, egal, ob Sie es
nutzen oder nicht.  Benutzer mit OldWorld-Systemen sollten HFS einschalten, um
den kompilierten Kernel auf die MacOS-Partition zu kopieren.</warn>

<impo>Wenn Sie einen ATAPI CD-Brenner benutzen, sollten Sie die SCSI-Emulation
im Kernel aktivieren.  Die neuen cdrtools können zwar direkt auf
ATAPI-Geräate ohne die SCSI-Simulation zugreifen, jedoch ist dieses noch
als "unstable" eingestuft.  Also, zur Aktivierung der SCSI-Emulation schalten
Sie <e>"ATA/IDE/MFM/RLL support" --> "IDE, ATA and ATAPI Block devices" -->
"SCSI emulation support" (ruhig als Modul)</e> ein; dann unter <e>"SCSI support"
--> "SCSI suppport", "SCSI CD-ROM support" und "SCSI generic support" (ebenso
als Modul)</e> einschalten.  Falls Sie Module verwenden wollen, müssen Sie
noch nach der Kompilation des Kernels <c>echo -e "ide-scsi\nsg\nsr_mod" &gt;&gt;
/etc/modules.autoload</c> eingeben, um die Module automatisch beim Booten zu
laden.  Sie sollten außerdem <c>append="hdc=ide-scsi"</c> in Ihre
<path>/etc/yaboot.conf</path> hinzufügen.</impo>

<impo>Wenn Sie ein OldWorld-System besitzen, mounten Sie ihre HFS (MacOS)
Partition, dann kopieren Sie den Kernel darauf, sodass BootX ihn beim Starten
laden kann.  BootX sucht automatisch nach Kernel im Verzeichnis <path>Linux
Kernels</path> im System-Verzeichnis.</impo>

<p>Ihr neuer, angepasster Kernel (und die Module) sind nun installiert.  Sie
müssen nun einen "System Logger" zur Installation wählen.  Wir
bieten mit <e>sysklogd</e> das traditionelle Paket an "system logging daemons".
Ebenso gibt es <e>metalog</e>, zu dem Power-User von sysklogd aufgrund seiner
schlechten Leistung umsteigen.  Wenn Sie sich nicht sicher sind, versuchen Sie
metalog, da es inzwischen ziemlich populär zu sein scheint.  Um einen
Logger zu installieren, tippen Sie <e>eine</e> der folgenden Möglichkeiten
ein:</p>

<pre>
# <e>emerge app-admin/sysklogd</e>
# <e>rc-update add sysklogd default</e>
<comment>oder</comment>
# <e>emerge app-admin/metalog</e>
# <e>rc-update add metalog default</e>
</pre>

<warn>Falls Sie ein Version 1.2 stage3-Archiv verwenden, brauchen Sie keinen
System Logger zu installieren.  stage3-ppc-1.2.tbz2 beinhaltet sysklogd.  Wenn
Sie stattdessen metalog verwenden möchten, müssen Sie sysklogd sauber
deinstallieren:  Teilen Sie zuerst dem System mit, dass Sie sysklogd nicht
weiter beim Booten starten möchten: <c>rc-update del sysklogd</c>.  Dann
deinstallieren Sie sysklogd mit <c>emerge unmerge sysklogd</c>.
Möglicherweise müssen Sie noch die sysklogd-Konfigurationsdatei in
<path>/etc</path> löschen (halten Sie nach <c>cfgpro</c> in der Dateiliste
beim deinstallieren Ausschau).  Nun können Sie metalog mit <c>emerge
metalog</c> installieren und durch <c>rc-update add metalog default</c>
automatisch beim Booten starten.</warn>

<impo>metalog schreibt seine Daten blocksweise auf die Festplatte, weshalb
Nachrichten nicht direkt in den Logdateien auftauchen.  Falls Sie einen Daemon
debuggen möchten, ist dieses Verhalten weniger sinnvoll.  Während Ihr
Gentoo Linux System läuft, können Sie metalog ein USR1-Signal senden,
um vorrübergehend die Pufferung von Nachrichten abzuschalten (sprich,
<c>tail -f /var/log/everything/current</c> arbeitet nun wie erwartet in
Realzeit); ein USR2-Signal schaltet die Pufferung wieder ein.</impo>

<p>Sie können jetzt ein beliebiges Cron Paket zur Installation
auswählen.  Momentan bieten wir <c>dcron</c>, <c>fcron</c> und
<c>vcron</c>.  Wenn Sie sich nicht entscheiden können, wählen Sie
<c>vcron</c>.  Eines der Pakete installieren Sie wie folgt:</p>

<pre>
# <e>emerge sys-apps/dcron</e>
# <e>crontab /etc/crontab</e>
<comment>oder</comment>
# <e>emerge sys-apps/fcron</e>
# <e>crontab /etc/crontab</e>
<comment>oder</comment>
# <e>emerge sys-apps/vcron</e>
# <e>crontab /etc/crontab</e>
</pre>

<p>Für weitere Informationen, wie Cron unter Gentoo Linux funktioniert,
lesen Sie <uri
link="http://lists.gentoo.org/pipermail/gentoo-announce/2002-April/000151.html">diese
Nachricht</uri>.</p>

</body>
</section>
</chapter>

<chapter>
<title>Letzte Schritte: Zusätzliche Pakete installieren</title>
<section>
<body>

<p>Wenn Sie Benutzer eines Laptops sind und Ihre PCMCIA-Slots nach dem ersten
richtigen Booten benutzen wollen, sollten Sie das Paket <c>pcmcia-cs</c>
installieren.</p>

<pre>
# <e>emerge sys-apps/pcmcia-cs</e>
</pre>
</body>
</section>
</chapter>


<chapter>
<title>Letzte Schritte: Konfiguration des Systems</title>

<section>
<title><path>/etc/fstab</path></title>
<body>

<p>Ihr Gentoo Linux System ist nun annähernd betriebsbereit.  Es
müssen noch einige wichtige Systemdateien konfiguriert und der yaboot
Bootmanager installiert werden.</p>

<p>Zuerst muss die Datei <path>/etc/fstab</path> angepasst werden.  Tragen Sie
die Dateisysteme <c>ext2</c> oder <c>ext3</c> entsprechend Ihrer Konfiguration
ein.  Verwenden Sie das unten gelistete Beispiel und ersetzen Sie <c>ROOT</c>
und <c>SWAP</c> durch die entsprechenden Geräte (z.B. <c>hda2</c> usw.).
Sind Sie Benutzer eines NewWorld-Systems, tragen Sie <e>nicht</e> die bootstrap
Partition ein!</p>

<pre caption="/etc/fstab">
<comment>
# /etc/fstab: static file system information.
#
# "noatime" schaltet "atimes" (man mount) ab und erhöht so die Performance. ("atimes"
# wird normalerweise nicht benötigt und "notail" erhöht die Leistung eines ReiserFS
# Dateisystems, allerdings auf Kosten der Speicherausnutzung.) Wenn Sie frei zwischen
# "tail" oder "notail" wählen wollen, lassen Sie die "noatime" Option sicherheitshalber
# weg.
# &lt;fs&gt;           &lt;mountpoint&gt;   &lt;type&gt;   &lt;opts&gt;          &lt;dump/pass&gt;
</comment>
/dev/ROOT           /           ext3	 noatime         0 1
/dev/SWAP           none        swap	 sw              0 0
/dev/cdroms/cdrom0  /mnt/cdrom  iso9660	 noauto,ro       0 0
proc                /proc       proc	 defaults        0 0
</pre>
</body>
</section>

<section>
<title>Das Passwort für den Superuser root setzen</title>
<body>

<p>Bevor Sie es vergessen, setzen Sie das Passwort für den Superuser root
mittels: </p>

<pre>
# <e>passwd</e>
</pre>
</body>
</section>

<section>
<title><path>/etc/hostname</path></title>
<body>

<p>Tragen Sie in dieser Datei den kompletten Namen (Rechnername und Domainname)
für Ihren Rechner in iener einzigen Zeile ein, z.B.
<c>rechnername.domainname.lan</c>.</p>

</body>
</section>

<section>
<title><path>/etc/hosts</path></title>
<body>

<p>Diese Datei enthält eine Auflistung von IP-Adressen und Ihrem
zugehörigen Rechnernamen.  Sie wird vom System gebraucht, um IP-Adressen
von Rechnernamen aufzulösen, die nicht Ihrem verwendeten Nameserver stehen.
Hier ist ein Beispiel der Datei:</p>

<pre caption="/etc/hosts">
127.0.0.1      localhost
<comment>
# Die folgende Zeile enthält eine IP-Adresse ihres lokalen
# Netzwerks und dem Ihrem Rechner zugewiesenen <uri link="#doc_chap15_sect3">Rechnernamen</uri>
</comment>
192.168.1.1    rechnername.domainname.lan	rechnername
</pre>

</body>
</section>

<section>
<title>Abschließende Netzwerkkonfiguration</title>
<body>

<p>Fügen Sie die Namen aller Module, die für den Betrieb Ihres System
benötigt werden, in die Datei <path>/etc/modules.autoload</path> ein
(Optionen können Sie in derselben Zeile an den Modulnamen anhänten).
Wenn Gentoo Linux bootet, werden diese Module automatisch geladen.  Vor allem
sollten Sie dort das Modul der Netzwerkkarte eintragen, falls Sie es als Modul
kompiliert haben: </p>

<pre caption="/etc/modules.autoload">
sungem
</pre>

<p>Passen Sie vor dem ersten Booten die Datei <path>/etc/conf.d/net</path> Ihrer
Netzwerkumgebung an:</p>

<pre>
# <e>nano -w /etc/conf.d/net</e>
# <e>rc-update add net.eth0 default</e>
</pre>

<p>Besitzen Sie mehrere Netzwerkkarten, müssen Sie weitere
<path>net.eth<comment>x</comment></path> Skripte für jede Karte
(<comment>x</comment> = 1, 2, ...) erstellen:</p>

<pre caption="Mehrere Netzwerkkarten">
# <e>cd /etc/init.d</e>
# <e>cp net.eth0 net.eth<comment>x</comment></e>
# <e>rc-update add net.eth<comment>x</comment> default</e>
</pre>

<p>Haben Sie eine PCMCIA-Karte installiert, prüfen Sie, ob die
Einstellungen in der Datei <path>/etc/init.d/pcmcia</path> für Ihr System
korrekt sind und fügen dann
<pre>
depend() {
	need pcmcia
}
</pre>
am Kopf der Datei <path>/etc/init.d/net.eth<comment>x</comment></path> ein, sodass
PCMCIA-Treiber automatisch geladen werden, wenn die Netzwerkeinstellungen
geladen werden.</p>

<p>Wechseln Sie öfter zwischen diversen Netzen (wenn Sie z.B. Ihren Laptop
daheim und auf der Arbeit verwenden), wird Ihnen sicherlich das Programm
<c>quickswitch</c> gefallen.  Dort können Sie diverse Umgebungen anlegen
und schnell zwischen diesen wechseln, ohne jedesmal die Konfigurationsdateien
anzupassen.</p>

</body>
</section>

<section>
<title>Grundlegende Einstellungen (einschließlich des Tastaturlayouts)</title>
<body>
<pre caption="Grundlegende Einstellungen">
# <e>nano -w /etc/rc.conf</e>
</pre>

<p>Folgen Sie den Anweisungen in der Datei, um die Einstellungen anzupassen.
Überprüfen Sie, dass <c>CLOCK</c> Ihrer Vorliebe entspricht.  Das
Tastaturlayout kann über die Variable <c>KEYMAP</c> eingestellt werden (in
<path>/usr/share/keymaps/</path> finden Sie verschiedene Möglichkeiten;
auch Mac-Benutzer sollten nun die Konfiguration aus dem Verzeichnis
<path>i386</path> verwenden).  Für eine deutsche Tastaturbelegung
können Sie folgendes einstellen:</p>

<pre caption="Deutsche Tastaturbelegung">
<e>KEYMAP="de-latin1"</e>
</pre>

</body>
</section>
</chapter>


<chapter>
<title>Letze Schritte: Konfiguration des Bootmanagers</title>

<section>
<body>
<p>Der größte Unterschied von Linux auf x86-Maschinen ist der
Bootloader.  NewWorld-Mschinen benutzen <c>yaboot</c>.  OldWorld-Maschinen
verwenden BootX.</p>

</body>
</section>

<section>
<title><c>yaboot</c> für NewWorld-Maschinen (automatische Konfiguration mit <c>yabootconfig</c>)</title>
<body>

<p>Sie können das neue und verbesserte Programm <c>yabootconfig</c>
verwenden, das bei <c>yaboot-1.3.8-r1</c> und neueren Versionen beiliegt, um
automatisch <c>yaboot</c> zu Konfigurieren.  <c>yabootconfig</c> wird die
diversen Partitionen Ihres Rechners finden und eine Möglichkeit des Dual-
oder Triple-Boot mit Linux, Mac OS und Mac OS X bereitstellen.</p>

<p>Um <c>yabootconfig</c> zu verwenden, muss Ihre Festplatte ine bootstrap
Partition besitzen und die Datei <path>/etc/fstab</path> entsprechend Ihrer
Linux Partitionen konfiguriert sein.  Das sollten Sie aber schon in den
vorangegangenen Schritten erledigt haben.  Bevor Sie starten, installieren Sie
die letzte verfügbare Version von <c>yaboot</c>, indem Sie <c>emerge -u
yaboot</c> eingeben.  Das ist nötig, da die neueste Version immer in
Portage verfügbar ist, aber nicht auf den Installations-Archiven.</p>

<p>An der Eingabeaufforderung geben Sie <c>yabootconfig</c> ein.  Das Programm
startet nun und es verlangt von Ihnen die Bestätigung, ob die gefundene
bootstrap Partition die richtige ist.  Geben Sie <c>Y</c> ein, falls es korrekt
ist.  Falls nicht, überprüfen Sie Ihre <path>/etc/fstab</path>.
<c>yabootconfig</c> wird dann überprüfen, ob andere Betriebssysteme
auf Ihrem Rechner installiert sind, die Datei <path>/etc/yaboot.conf</path>
erstellen und <c>mkofboot</c> ausführen (mkofboot wird zur Formatierung der
bootstrap Partition und Installation der yaboot Konfiguration dorthin
verwendet).</p>

<p>Sie sollten die Datei <path>/etc/yaboot.conf</path> auf Korrektheit
überprüfen.  Wenn Sie Änderungen an <path>/etc/yaboot.conf</path>
vorgenommen haben (wie z.B. Änern des automatisch zu bootenden
Betriebssystems), führen Sie darauf <c>ybin -v</c> aus, um Ihre bootstrap
Partition zu erneuern.</p>

</body>
</section>

<section>
<title><c>yaboot</c> für NewWorld-Maschinen (manuelle Konfiguration von <path>/etc/yaboot.conf</path>)</title>
<body>

<p>Wenn Sie aus irgendeinem Grund nicht <c>yabootconfig</c> die
Konfigurationsdatei <path>/etc/yaboot.conf</path> erstellen lassen wollen,
können Sie die bereits vorhandene Beispieldatei anpassen.  Hier eine
mögliche <path>yaboot.conf</path>:</p>

<pre caption="/etc/yaboot.conf">
<comment>
## /etc/yaboot.conf
##
## Rufen Sie "man yaboot.conf" für Details auf.  Machen Sie keine
## Änderungen, außer, Sie benötigen Sie wirklich!!
## Sehen Sie sich auch in /usr/share/doc/yaboot/example mögliche
## Konfigurationen an.
##
## Für ein Dual-Bootmenü, fügen Sie eine oder mehrere Zeilen wie
## bsd=/dev/hdaX, macos=/dev/hdaY, macosx=/dev/hdaZ  hinzu.
</comment>

<comment>## Unsere bootstrap Partition:</comment>
boot=/dev/hda9

<comment>
## hd: ist für die OpenFirmware, um hda anzusprechen.
## führen Sie ofpath /dev/hda aus, um die richtigen Einstellung zu finden
</comment>
device=hd:
<comment>## Partition, auf der die Kernel-Images zu finden sind</comment>
partition=11

root=/dev/hda11
delay=5
defaultos=macosx
timeout=30
install=/usr/lib/yaboot/yaboot
magicboot=/usr/lib/yaboot/ofboot

<comment>
#################
## Dieser Abschnitt kann dupliziert werden, um zwischen mehreren Kernel
## oder unterschiedlichen Boot-Optionen wählen zu können
#################
</comment>
	image=/boot/vmlinux
	label=Linux
	sysmap=/boot/System.map
	read-only
<comment>##################</comment>

macos=/dev/hda13
macosx=/dev/hda12
enablecdboot
enableofboot
</pre>

<p>Sobald Sie die Datei <path>yaboot.conf</path> Ihren Wünschen
entsprechend konfiguriert haben, führen Sie <c>mkofboot -v</c> aus, um die
Einstellungen in der bootstrap Partition zu sichern.  Vergessen Sie es nicht!
Wenn alles gut verläuft und Sie dieselben Optionen wie in dem Beispiel oben
verwendet haben, sehen Sie nach dem nächsten Reboot ein einfaches
Bootmenü mit fünf Einträgen.  Sollten Sie später die Datei
<path>/etc/yaboot.conf</path> ändern, müssen Sie <c>ybin -v</c>
ausführen, um die bootstrap Partition zu aktualisieren (mkofboot wird nur
beim ersten Mal benötigt).</p>

<note><c>yaboot</c> bestizt die Möglichkeit, einige Einstellungen
während des Bootvorgangs vorzunehmen.  Schauen Sie für weitere
Informationen auf <uri>http://penguinppc.org/projects/yaboot/</uri></note>

</body>
</section>

<section>
<title>BootX für OldWorld-Maschinen</title>
<body>

<impo>Führen Sie diese Schritte nach dem Reboot Ihres Rechners im
nächsten Schritt aus.</impo>

<p>Booten Sie Ihren Rechner in MacOS und öffnen Sie die BootX Erweiterung.
Wählen Sie <c>Options...</c> und schalten Sie <c>Use specified RAM Disk</c>
aus.  Wenn Sie zu den BootX Haupteinstellungen zurückkehren, werden Sie nun
eine Option vorfinden, in der Sie Ihre Festplatte und Partition der Root
Partition angeben.  Geben Sie dort die entsprechenden Werte ein.</p>

<p>BootX kann so eingestellt werden, dass Linux beim Booten gestartet wird.
Dabei wird erst Ihr Rechner in MacOS gestartet, worauf BootX Linux lädt und
startet.  Schauen Sie auf die <uri
link="http://penguinppc.org/proejcts/bootx/">BootX Homepage</uri> für
weitere Informationen.</p>

</body>
</section>

</chapter>

<chapter>
<title>Installation beendet!</title>
<section>
<body>

<p>Gentoo Linux ist installiert.  Der letzte verbleibende Schritt ist die
chroot-Umgebung zu verlassen, die Partitionen sicher zu unmounten und den
Rechner zu reboooten:</p>

<pre>
# <e>exit</e>  <comment>Dieses beendet die chroot Umgebung; Sie können auch <c>^D</c> eingeben</comment>
# <e>cd / </e>
# <e>umount /mnt/gentoo/proc</e>
# <e>umount /mnt/gentoo</e>
# <e>reboot</e>
</pre>

<note>Nach dem Reboot sollten Sie <c>update-modules</c> ausführen, um die
Datei <path>/etc/modules.conf</path> zu erzeugen.  Anstatt die Datei direkt
editieren, sollten Sie Änderungen in <path>/etc/modules.d</path> vornehmen.</note>

</body>
</section>
</chapter>

<chapter>
<title>Extras</title>
<section>
<title>Emulierung von meheren Maustasten</title>
<body>

<p>Sie können die folgenden Zeilen der Datei <path>/etc/sysctl.conf</path>
hinzufügen, um mehrere Maustasten bei jedem Booten zu emulieren.  Die Werte
für die Tasten können Sie mit dem Programm <c>showkey</c> ermitteln.
Hier ist ein Beispiel, um F11 und F12 zu verwenden:</p>

<pre>
# Aktiviert die Maustasten-Emulation
dev.mac_hid.mouse_button_emulation = 1
# Setze zweite Maustaste auf 87 - F11
dev.mac_hid.mouse_button2_keycode = 87
# Setze dritte Maustaste auf 88 - F12
dev.mac_hid.mouse_button3_keycode = 88
</pre>

</body>
</section>

<section>
<title>Gentoo-Stats</title>
<body>

<p>Das "Gentoo Linux usage statistics project" wurde gestartet, um den
Entwicklern einen Überblick der Gentoo-Benutzer zu verschaffen.  Es sammelt
Informationen über die Verbreitung von Gentoo Linux und hilft,
Prioritäten bei den Entwicklern zu setzten.  Die Installation ist
freiwillig, jedoch wäre es willkommen, wenn Sie daran beteiligen
würden.  Generierte Stastiken können Sie sich unter
<uri>http://stats.gentoo.org/</uri> ansehen.</p>

<p>Der gentoo-stats Server wird Ihrem System eine eindeutige ID vergeben.  Diese
ID wird dafür benötigt, dass jedes System nur einmal gezält wird.
Diese ID wird weder dafür verwendet, um ihr System individuell zu
identifiziern, noch wird sie mit der IP-Adresse oder anderen persönlichen
Informationen abgeglichen.  Alle Vorkehrungen wurde getroffen, um Ihre
Privatssphäre in der Entwicklung dieses Systems zu wahren.  Nachfolgend
sind die Informationen geliestet, die das <c>gentoo-stats</c> Programm
überwachen wird:</p>

<ul>
<li>installierte Pakete und ihre Versionsnummer</li>
<li>CPU Informationen: Geschwindigkeit (MHz), Hersteller, Modell, CPU Flags (wie "altivec"</li>
<li>Speicher Informationen (installierter physischer RAM, Größe der angelegten Swap Partition(en))</li>
<li>PCI Karten und Netzwerkkarten Chipsätze</li>
<li>das Gentoo Linux Profil, das Ihr Rechner verwendet (dorthin, wo der Link <path>/etc/make.profile</path> zeigt)</li>
</ul>

<p>Wir sind uns bewusst, dass eine Bekanntmachung von sensiblen Informationen
eine Gefährdung für viele Gentoo Linux Benutzer darstellt (und ebenso
für die Entwickler).</p>

<!-- Ich hoffe die Aussage des Satzes richtig getroffen zu haben:
We are aware that disclosure of sensitive information is a threat to most Gentoo Linux users (just as it is to the developers).-->

<ul>
<li>Solange Sie nicht das gentoo-stats Programm ändern, wird niemals eine
sensible Information, wie Ihr Passwort, Konfigurationsdateien, der
Lieblingsfilm, ... übertragen</li>
<li>Die Übermittlung Ihrer E-Mail Adresse ist optional und
defaultmäßig ausgeschaltet.</li>
<li>Die IP-Adresse von der Ihre Datenübertragung stammt wird niemals
in der Art gespeichert, dass wir Sie identifizieren können.  Es gibt keine
Zusammenänge zwischen der IP-Adresse und der System-ID.</li>
</ul>

<p>Die Installation ist einfach -- fürhen Sie die folgenden Befehle
aus:</p>

<pre caption="gentoo-stats installieren">
# <c>emerge gentoo-stats</c>   <comment>Installiert gentoo-stats</comment>
# <c>gentoo-stats --new</c>    <comment>Bezieht eine neue System-ID</comment>
</pre>

<p>Der zweite Befehl wird eine neue System-ID anfragen und sie in
<path>/etc/gentoo-stats/gentoo-stats.conf</path> ablegen.  Sie können
diese Datei anschauen, um zusätzliche Konfigurationsparameter zu sehen.</p>

<p>Sie sollten das Programm regelmäßig laufen lassen (gentoo-stats
benötig keine Superuser Rechte).  Fügen Sie folgende Zeile in Ihre
<path>crontab</path> ein:</p>

<pre caption="gentoo-stats mit cron updaten">
<c>0 0 * * 0,4 /usr/sbin/gentoo-stats --update > /dev/null</c>
</pre>

<p>Das <c>gentoo-stats</c> Programm ist ein einfaches Perl-Script, dass Sie sich
mit Ihrem bevorzugten Textbetrachter oder Editor anschauen können:
<path>/usr/sbin/gentoo-stats</path>.</p>

</body>
</section>

<section>
<title>Weiterführende Informationen</title>
<body>

<p>Wenn Sie Fragen haben oder bei der Entwicklung von Gentoo Linux mithelfen
möchten, tragen Sie sich auf der gentooppc-user und gentooppc-dev
Mailing-Liste ein (es gibt Links dazu auf unserer <uri
link="/main/en/lists.xml">Mailinglisten-Seite</uri> (bisher nur in Englisch).</p>

<p>Außerdem gibt es eine <uri link="/doc/de/desktop.xml">Anleitung zur
Gentoo Linux Desktop Konfiguration</uri>, die Ihnen bei folgenden Konfiguration
Ihres neuen Gentoo Linux Systems weiterhelfen wird.  Weiterhin ist der <uri
link="/doc/de/portage-user.xml">Portage Benutzer Leitfaden</uri> für eine
Einführung in Portage empfehlenswert.</p>

<p>Die <uri link="/doc/en/gentoo-ppc-faq.xml">Gentoo Linux/PowerPC FAQ</uri>
(derzeit nur in Englisch) behandelt oft gestellte Fragen zum Betrieb von Gentoo
Linux/PPC.  Die allgemeine <uri link="/doc/de/faq.xml">Gentoo Linux FAQ</uri>
wird Ihnen vielleicht auch weiterhelfen, obwohl sie sich hauptsächlich an
Benutzer von x86-Architekturen richtet.  <uri
link="http://forums.gentoo.org/viewforum.php?f=24">Gentoo on PPC</uri> (auf
forums.gentoo.org) ist ein offenes Brett für Diskussionen rund um alle
PowerPC Angelegenheiten.  Für gewöhnlich gibt es direkte Diskussionen
im Chat-Kanal <c>#gentoo-ppc</c> auf dem IRC-Server <e>irc.freenode.net</e>.</p>

<p>Herzlich Willkommen und viel Spaß mit Gentoo Linux!</p>

</body>
</section>

</chapter>

</guide>
